var $ = require('jquery');

$(function() {
	$('body').html('If you can read this, then javascript is loaded and running correctly. <br /><br /><span>If you can also read this, then stylesheets are compiling correctly, too!</span>');
});
